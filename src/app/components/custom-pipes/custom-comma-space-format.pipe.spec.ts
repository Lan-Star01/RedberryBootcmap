import { CustomCommaSpaceFormatPipe } from './custom-comma-space-format.pipe';

describe('CustomCommaSpaceFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomCommaSpaceFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
